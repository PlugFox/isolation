@internal

import 'package:meta/meta.dart';

/// Enable logging key
const Symbol kLogEnabled = #isolation.log.enabled;

/// Isolate debug name key
const Symbol kIsolateDebugName = #isolation.isolate.name;
